import streamlit as st
import numpy as np
import matplotlib.pyplot as plt
from matplotlib import cm
import io

# 1. НАЛАШТУВАННЯ СТОРІНКИ
st.set_page_config(page_title="Мандала особистості", layout="wide")

st.title("🎨 Цифрова мандала особистості")
st.write("### Математично-мистецький проєкт. Візуалізація персональних даних")
st.markdown("---")

# 2. ВКЛАДКИ
tab1, tab2 = st.tabs(["🚀 Генератор мандали", "📜 Наукове обґрунтування"])

with tab2:
    st.header("Математична декомпозиція моделі")
    st.write("""
    Проєкт базується на побудові багатошарової графічної системи в полярних координатах $(r, \\theta)$. 
    Кожна лінія є результатом обчислення функції, де аргументами виступають вхідні дані користувача.
    """)

    # --- ГЛОБАЛЬНИЙ МАСШТАБ ---
    st.subheader("1. Глобальне масштабування")
    st.write("Для приведення фізичних величин до одиниць графічного полотна використовується коефіцієнт нормалізації:")
    st.latex(r"S_{global} = \frac{1}{\frac{H}{100} + \frac{S}{5} + 6}")
    st.write("Це гарантує стабільність композиції незалежно від вхідних даних.")

    # --- ЦЕНТРАЛЬНЕ КІЛЬЦЕ ---
    st.subheader("2. Центральне кільце")
    st.write("Кільце символізує базову опору особистості. Його радіус та ширина залежать від показника сну ($S$):")
    st.latex(r"r_{in} = \frac{S}{5} \cdot S_{global}")
    st.latex(r"r_{out} = r_{in} + (0.05 + 0.08 \cdot S) \cdot S_{global}")
    st.write("Показник сну ($S$) керує двома векторами: зміщенням кільця від центру та його фізичною товщиною (щільністю ресурсу).")

    # --- ПЕЛЮСТКИ ---
    st.subheader("3. Шар пелюсток")
    st.write("Форма пелюсток визначається місяцем народження ($n$) та рівнем Впевненості ($E$):")
    st.latex(r"r(\theta) = r_{out} + 0.4 + \left| \cos\left(\frac{n}{2}\theta\right) \right|^p \cdot 2.5 \cdot S_{global}")
    st.write("Де показник степеня $p = (11 - E) / 2$. При збільшенні $E$ (Впевненості) форма трансформується від м'якої до вектороподібної (гострої).")

    # --- СПІРАЛІ ---
    st.subheader("4. Поле спіралей")
    st.write("Вік ($A$) визначає кількість шарів. Кожна лінія обертається на кут $\Phi$, що залежить від Енергії ($T$) та Статі ($G$):")
    st.latex(r"\Phi_{rot} = G \cdot \left( i \cdot \frac{T}{10} \cdot \pi \right)")
    st.write("Параметр $G$ діє як інвертор знаку, змінюючи напрямок обертання всієї системи (дзеркальна симетрія).")

    # --- КОРОНА ---
    st.subheader("5. Зовнішня межа")
    st.write("Щоб підкреслити характер вектора, використано степеневу модуляцію:")
    st.latex(r"R_{border} = R_{max} + 0.5 \cdot S_{global} \cdot |\sin(d \cdot \theta)|^p")
    st.write("""
    * **Чоловічий вектор ($p=0.4$):** створює структурність.
    * **Жіночий вектор ($p=1.5$):** створює органічність.
    """)
    
    # --- СЕКТОРИ (НОВЕ) ---
    st.subheader("6. Секторальна сітка")
    st.write("Мандала розбивається на сектори, кількість яких відповідає Місяцю народження ($n$).")
    st.latex(r"\theta_{sector} = \frac{2\pi}{n} \cdot k")
    st.write("Лінії секторів виконуються контрастним (комплементарним) кольором для створення чіткої системи координат.")

    # --- ТЕМПЕРАМЕНТ ---
    st.subheader("7. Графічна інтерпретація темпераменту")
    st.write("Темперамент визначає візуальну щільність та 'фізику' лінії:")
    st.write("""
    * **Холерик:** Яскрава, непрозора лінія (імпульсивність).
    * **Флегматик:** Масивна, широка, але напівпрозова лінія (інертність та об'єм).
    * **Сангвінік:** Збалансована чітка лінія середньої товщини (стабільність).
    * **Меланхолік:** Тонка пунктирна лінія (висока сензитивність).
    """)
    
with tab1:
    # ПАНЕЛЬ КЕРУВАННЯ
    st.sidebar.header("📋 Твої дані")
    with st.sidebar:
        n = st.number_input("Місяць народження (Кількість секторів)", 1, 12, 6)
        d = st.number_input("День народження", 1, 31, 15)
        H = st.slider("Зріст (см)", 100, 220, 170)
        A = st.slider("Вік", 10, 100, 45)
        S = st.slider("Годин сну", 0, 15, 8)
        
        st.markdown("---")
        E = st.slider("Впевненість", 0, 10, 5, help="Змінює ГОСТРОТУ пелюсток.")
        T = st.slider("Енергія", 0, 10, 5, help="Змінює ЗАКРУЧЕНІСТЬ спіралі.")
        
        st.markdown("---")
        show_sectors = st.checkbox("🌐 Показати сектори", value=True)
        
        G = st.radio("Стать", options=[1, -1], format_func=lambda x: "Чоловіча" if x == 1 else "Жіноча")
        temp = st.selectbox("Темперамент", options=["Сангвінік", "Холерик", "Флегматик", "Меланхолік"])
        eye_choice = st.selectbox("Колір очей", options=[1, 2, 3, 4], 
                                    format_func=lambda x: {1:"Блакитні", 2:"Зелені", 3:"Карі", 4:"Янтарні"}[x])

    # ГРАФІЧНА ЛОГІКА
    def generate_mandala():
        style_map = {
            "Сангвінік": {"lw": 2.0, "alpha": 0.8, "ls": "-", "f_alpha": 0.4},
            "Холерик":   {"lw": 4.0, "alpha": 1.0, "ls": "-", "f_alpha": 0.6},
            "Флегматик": {"lw": 6.0, "alpha": 0.4, "ls": "-", "f_alpha": 0.2},
            "Меланхолік": {"lw": 0.8, "alpha": 0.8, "ls": "--", "f_alpha": 0.2}
        }
        s = style_map[temp]
        
        color_maps = {1: cm.winter, 2: cm.summer, 3: cm.autumn, 4: cm.spring}
        selected_cmap = color_maps.get(eye_choice, cm.plasma)
        
        # ВИЗНАЧЕННЯ КОНТРАСТНОГО КОЛЬОРУ ДЛЯ СЕКТОРІВ
        # 1-Блакитні (Зима) -> Золотий
        # 2-Зелені (Літо) -> Маджента
        # 3-Карі (Осінь) -> Ціан
        # 4-Янтарні (Весна) -> Фіолетовий
        contrast_colors = {
            1: '#FFD700', # Gold
            2: '#FF00FF', # Magenta
            3: '#00FFFF', # Cyan
            4: '#9400D3'  # Dark Violet
        }
        sector_color = contrast_colors.get(eye_choice, 'white')
        
        t = np.linspace(0, 2 * np.pi, 2500)
        
        fig = plt.figure(figsize=(6, 6), facecolor='black')
        ax = plt.subplot(111, projection='polar')
        ax.set_facecolor('black')
        
        global_scale = 1 / (H/100 + S/5 + 6)
        
        # 1. ЦЕНТРАЛЬНЕ КІЛЬЦЕ
        r_in = (S / 5) * global_scale
        r_thickness = (0.05 + S * 0.08) * global_scale 
        r_out = r_in + r_thickness
        
        ax.fill_between(t, r_in, r_out, color=selected_cmap(0.9), alpha=(s["f_alpha"] + 0.3))
        ax.plot(t, np.full_like(t, r_in),
